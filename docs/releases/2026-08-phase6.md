# Phase 6：长期维护基线

完成日期：2026-08-03

## 已完成

- 使用 pip-tools 固化生产与开发依赖闭包及 SHA256 哈希；Python 基础镜像固定 digest，生产安装启用 `--require-hashes`。
- CI 增加 Ruff、mypy、pytest、ShellCheck、pip-audit、OpenAPI snapshot、Trivy runtime 镜像门禁和 CycloneDX SBOM artifact。
- 升级 FastAPI、Starlette、cryptography 与 python-multipart；依赖审计无已知漏洞。runtime 镜像移除不需要的 pip、setuptools 和 wheel，Trivy 的 HIGH/CRITICAL 扫描为零。
- 当前契约、未来 RFC/审查与历史 release 文档分层；根目录旧链接保留兼容跳转。
- HTTP 认证/限流/请求体保护进入 `app/http.py`；上传和严格删除状态编排进入 `app/uploads.py` 与 `app/deletions.py`，FastAPI 路由只负责输入输出和委派。
- `Database` 继续作为 SQLite repository/迁移边界，`Runtime` 继续作为 Provider、恢复和后台生命周期边界；没有为目录对称做无行为收益的进一步拆分。
- 移除工作区 Windows Zone.Identifier 元数据。Git 对象库约 12.39 MiB，最大受控文件约 1.2 MiB，因此不进行会中断协作的历史重写。
- 暂不增加 `THIRD_PARTY_NOTICES.md`：Chart.js 已保留 MIT 许可头，CI SBOM 覆盖 Python 依赖；有独立法务交付要求时再生成统一 notices。

## 验收

- 容器自动测试：`101 passed`。
- Ruff check/format、mypy、ShellCheck、pip-audit 全部通过。
- OpenAPI snapshot 与应用输出一致。
- runtime 镜像 Trivy HIGH/CRITICAL：0。
